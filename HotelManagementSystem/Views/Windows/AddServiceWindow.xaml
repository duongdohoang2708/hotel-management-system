<Window x:Class="HotelManagementSystem.Views.Windows.AddServiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Thêm dịch vụ" Height="540" Width="1000" WindowStartupLocation="CenterScreen">
    <Border Background="#fff" CornerRadius="14" Padding="18" Margin="8">
        <StackPanel>
            <TextBlock Text="Thêm dịch vụ" FontSize="28" FontWeight="Bold" Foreground="#039BE5" Margin="0,-8,0,17" HorizontalAlignment="Center"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- Bên trái: Danh sách dịch vụ -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Danh sách dịch vụ" FontWeight="Bold" Foreground="#039BE5" FontSize="16" Margin="0,0,0,4"/>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <ComboBox ItemsSource="{Binding ServiceCategoryList}" SelectedItem="{Binding SelectedCategory, Mode=TwoWay}" DisplayMemberPath="CategoryName" Width="130" Margin="0,0,8,0"/>
                        <TextBox Width="254" Margin="0,0,8,0" materialDesign:HintAssist.Hint="Tìm dịch vụ" Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <DataGrid ItemsSource="{Binding FilteredServiceList}" AutoGenerateColumns="False" Height="270" Margin="0,8,0,0" CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column" >
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Loại DV" Binding="{Binding Category.CategoryName}" Width="120" MinWidth="80"/>
                            <DataGridTextColumn Header="Dịch vụ" Binding="{Binding ServiceName}" Width="*" MinWidth="120"/>
                            <DataGridTextColumn Header="Giá" Binding="{Binding UnitPrice, StringFormat=N0}" Width="90" MinWidth="70"/>
                            <DataGridTemplateColumn Width="70" MinWidth="50">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button Command="{Binding DataContext.AddToSelectedCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Width="32" Height="32"
                                                    Background="#43A047" Foreground="White"
                                                    BorderThickness="0"
                                                    Padding="0"
                                                    ToolTip="Thêm">
                                                <TextBlock Text="+" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Button>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
                <!-- Bên phải: Dịch vụ đã chọn -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Dịch vụ đã chọn" FontWeight="Bold" Foreground="#039BE5" FontSize="16" Margin="0,0,0,4"/>
                    <DataGrid ItemsSource="{Binding SelectedServices}" AutoGenerateColumns="False" Height="270" Margin="0,37,0,0" CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Dịch vụ" Binding="{Binding ServiceName}" Width="*" MinWidth="120"/>
                            <DataGridTextColumn Header="Số Lượng" Binding="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="90" MinWidth="70"/>
                            <DataGridTextColumn Header="Thành tiền" Binding="{Binding TotalPrice, StringFormat=N0}" Width="110" MinWidth="90"/>
                            <DataGridTemplateColumn Width="60" MinWidth="50">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button Command="{Binding DataContext.RemoveSelectedCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Width="28" Height="28"
                                                    Background="#E53935" Foreground="White"
                                                    BorderThickness="0"
                                                    Padding="0"
                                                    ToolTip="Xóa">
                                                <TextBlock Text="✖" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Button>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Grid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
                <Button Content="Lưu" Command="{Binding ConfirmCommand}" Width="120" Height="38" Background="#43A047" Foreground="White" FontWeight="Bold" Margin="0,0,18,0"/>
                <Button Content="Thoát" Command="{Binding CancelCommand}" Width="120" Height="38" Background="#BDBDBD" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window> 