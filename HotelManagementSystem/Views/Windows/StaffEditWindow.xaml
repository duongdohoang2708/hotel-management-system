<Window x:Class="HotelManagementSystem.Views.Windows.StaffEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:HotelManagementSystem.Converters"
        Title="Thêm/Chỉnh sửa nhân viên" Height="420" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <converters:DateOnlyToDateTimeConverter x:Key="DateOnlyToDateTimeConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Họ tên:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox Text="{Binding FullName}" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Text="Chức vụ:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox Text="{Binding Position}" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Text="SĐT:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox Text="{Binding Phone}" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Text="Email:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox Text="{Binding Email}" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Text="Ngày sinh:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <DatePicker SelectedDate="{Binding DateOfBirth, Mode=TwoWay, Converter={StaticResource DateOnlyToDateTimeConverter}}" Grid.Row="4" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Text="Giới tính:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <ComboBox ItemsSource="{Binding Genders}" SelectedItem="{Binding Gender, Mode=TwoWay}" Grid.Row="5" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Text="Địa chỉ:" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox Text="{Binding Address}" Grid.Row="6" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Text="CCCD:" Grid.Row="7" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox Text="{Binding CitizenId}" Grid.Row="7" Grid.Column="1" Margin="0,0,0,8" IsReadOnly="{Binding IsEditMode}"/>

        <TextBlock Text="Lương:" Grid.Row="8" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox Text="{Binding Salary}" Grid.Row="8" Grid.Column="1" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="9" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Lưu" Width="80" Margin="0,0,10,0" Command="{Binding SaveCommand}"/>
            <Button Content="Hủy" Width="80" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window> 