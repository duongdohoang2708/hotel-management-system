<Window x:Class="HotelManagementSystem.Views.Windows.SelectRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chọn phòng để thêm" Height="400" Width="500" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="RoomsDataGrid" Grid.Row="0" ItemsSource="{Binding Rooms}" SelectedItem="{Binding SelectedRoom, Mode=TwoWay}" AutoGenerateColumns="False" SelectionMode="Single" IsReadOnly="True"
                 ColumnHeaderStyle="{StaticResource CommonHeaderStyle}"
                 CellStyle="{StaticResource CommonCellStyle}"
                 RowStyle="{StaticResource CommonRowStyle}"
                 FontWeight="Normal"
                 VerticalContentAlignment="Center">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Số phòng" Binding="{Binding RoomNumber}" Width="*"/>
                <DataGridTextColumn Header="Loại phòng" Binding="{Binding RoomTypeName}" Width="*"/>
                <DataGridTextColumn Header="Giá" Binding="{Binding Price}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Chọn" Width="90" Margin="0,0,10,0" IsEnabled="{Binding SelectedRoom}" Click="SelectButton_Click"/>
            <Button Content="Hủy" Width="90" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 