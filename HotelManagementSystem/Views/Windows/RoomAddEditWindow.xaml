<Window x:Class="HotelManagementSystem.Views.Windows.RoomAddEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:HotelManagementSystem.Converters"
        Title="{Binding WindowTitle}" Height="296" Width="450" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

       

        <!-- RoomNumber -->
        <TextBlock Text="Số hiệu phòng:" Grid.Row="0" Grid.Column="0" Margin="0,8,8,0" VerticalAlignment="Top"/>
        <TextBox Text="{Binding RoomNumber}" Grid.Row="0" Grid.Column="1" Margin="0,5,0,25" Height="25"/>

        <!-- RoomType -->
        <TextBlock Text="Loại phòng:" Grid.Row="1" Grid.Column="0" Margin="0,8,0,0" VerticalAlignment="Top"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,6,0,25"
                  ItemsSource="{Binding RoomTypes}"
                  SelectedItem="{Binding SelectedRoomType}"
                  DisplayMemberPath="TypeName"
                  SelectedValuePath="RoomTypeId" Height="25"/>

        
        <!-- CleanStatus -->
        <TextBlock Text="Tình trạng dọn:" Grid.Row="2" Grid.Column="0" Margin="0,11,8,0" VerticalAlignment="Top"/>
        <ComboBox Grid.Row="2" Grid.Column="1" Margin="0,8,0,0"
                  ItemsSource="{Binding CleanStatusOptions}"
                  SelectedItem="{Binding CleanStatus}" Height="25"/>

        <!-- Nút Lưu và Hủy -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0" Width="184">
            <Button Content="Lưu" Width="80" Margin="0,0,25,0" Command="{Binding SaveCommand}" Style="{StaticResource ButtonBlueMedium}"/>
            <Button Content="Hủy" Width="80" Command="{Binding CancelCommand}" Style="{StaticResource ButtonPurpleMedium}"/>
        </StackPanel>
    </Grid>
</Window> 