<UserControl x:Class="HotelManagementSystem.Views.UserControls.SideBarUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:HotelManagementSystem.Converters"
             xmlns:models="clr-namespace:HotelManagementSystem.Models"
             Height="Auto" Width="220">
    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#222"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Setter Property="Padding" Value="20,14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="#1976D2"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#BBDEFB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Border Background="#FAFAFA" Padding="0,24,0,24">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="12" ShadowDepth="0" Opacity="0.12"/>
        </Border.Effect>
        <StackPanel>
            <!-- User Info -->
            <Border Background="#F0F0F0" Padding="20,16" Margin="0,0,0,16" CornerRadius="0,12,12,0">
                <StackPanel>
                    <TextBlock Text="{Binding StaffName}" Foreground="#222" FontWeight="Bold" FontSize="18"/>
                    <TextBlock Text="{Binding Role}" Foreground="#666" FontSize="14"/>
                    <Button Content="Đăng xuất" Command="{Binding LogoutCommand}" Margin="0,14,0,0" Style="{StaticResource SidebarButtonStyle}"/>
                </StackPanel>
            </Border>
            <Separator Margin="0,0,0,12" Background="#E0E0E0"/>
            <!-- TreeView Menu -->
            <TreeView ItemsSource="{Binding MenuItems}">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type models:MenuItemModel}" ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Icon}" FontSize="16" Width="32" TextAlignment="Center" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Title}" FontSize="16" FontWeight ="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="Margin" Value="0,6,0,6"/>
                        <EventSetter Event="MouseLeftButtonUp" Handler="TreeViewItem_MouseLeftButtonUp"/>
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>
        </StackPanel>
    </Border>
</UserControl>